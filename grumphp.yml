parameters:
    git_dir: .
    bin_dir: tools
    tasks:
        composer_normalize: ~
        composer_require_checker: ~
        phpcsfixer2:
            config: .php_cs
        psalm: ~
        jsonlint: ~
